{
  "name": "parallax-agent-ops",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "PARALLAX multi-agent operations with autonomous paper trading, live interaction streams, and a true Three.js/WebGL CORTEX agent persona.",
  "scripts": {
    "dev": "node scripts/local-server.js",
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "check": "node --check app.js && node --check cortex-3d.js && node --check scripts/build.mjs && node --check scripts/local-server.js && node --check api/run.js && node --check api/market.js && node --check api/wallet.js && node --check lib/orchestrator.js && node --check lib/trading.js"
  },
  "engines": {
    "node": "24.x"
  }
}
